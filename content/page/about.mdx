export const Highlight = ({children, color, test}) => (
  <span
    style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
    {test}
  </span>
);

<Highlight color="#005A9C">Dodger Blue</Highlight> is my favorite color.
