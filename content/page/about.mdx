export const Highlight = (props) => (
<>
  <span
    style={{
      backgroundColor: props.color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
    {props.children}
  </span>
{props.test}
</>
);

<Highlight color="#005A9C" >Dodger Blue</Highlight> is my favorite color.
